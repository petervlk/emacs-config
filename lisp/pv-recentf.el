;;; pv-recentf.el --- Summary

;;; Commentary:
;;; Setup config for showinf recent files
;;; Code:

(use-package recentf
  :config
  (recentf-mode 1))

(provide 'pv-recentf)

;;; pv-recentf.el ends here
